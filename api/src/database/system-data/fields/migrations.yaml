# brio_migrations isn't surfaced in the app, nor accessible from the API
table: brio_migrations

fields:
  - collection: brio_migrations
    field: version
  - collection: brio_migrations
    field: name
  - collection: brio_migrations
    field: timestamp
