# create-brio-extension

A small util that will scaffold a Brio extension.

## Installation

This package is meant to be used through `npm` or `yarn`:

```
npm init brio-extension
```

```
yarn create brio-extension
```
