{
    "name": "brio-age-encryption-module",
    "version": "0.1.0",
    "description": "Admin module to generate age keypairs and configure the recipient",
    "brio:extension": {
        "type": "module",
        "path": "./dist/index.js",
        "source": "./src/index.ts",
        "host": "^9.0.0"
    },
    "scripts": {
        "build": "mkdir -p dist && bun run ../../../packages/extensions-sdk/src/cli/run.ts build -i ./src/index.ts -o ./dist/index.js -t module",
        "dev": "bun run ../../../packages/extensions-sdk/src/cli/run.ts build -w --no-minify"
    },
    "devDependencies": {
        "@brio/extensions-sdk": "workspace:*",
        "typescript": "^5.7.3"
    }
}